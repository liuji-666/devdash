# DevDash i18n 多语言支持 + 拖拽修复

**目标**: 添加中英文切换 + 修复拖拽布局问题

## 用户反馈的问题

1. ✅ 窗口控制 — 已验证正常
2. ❌ 拖拽布局保存 — 保存正常但拖拽不了
3. ❌ 语言切换 — 需要添加中文/英文开关
4. ❌ PR 显示全是英文 — 看得懂但全是 English

## 已完成

### 1. i18n 系统 ✅

创建 `src/lib/i18n.ts`：
- 支持 Language 类型："en" | "zh"
- Zustand + persist 持久化语言设置
- 完整翻译词条（100+ 条）
- 分类：App、Sidebar、Dashboard、Widget、Settings、AI、Status、Common
- 使用 hook：`const { t, language, setLanguage } = useTranslation()`

### 2. 翻译词条覆盖

| 分类 | 词条示例 |
|------|----------|
| Widget | widget.prList → "合并请求" / "Pull Requests" |
| Widget | widget.merged → "已合并" / "merged" |
| Widget | widget.noData → "暂无数据" / "No data yet" |
| Settings | settings.language.zh → "中文" / "中文" |
| Settings | settings.language.en → "English" / "English" |
| Status | status.lastUpdate → "上次更新" / "Last update" |

### 3. 编译状态
- TypeScript (`tsc --noEmit`) ✅ 零错误
- Rust (`cargo check`) ✅ 零错误

## 待完成

- [ ] PlaceholderWidgets.tsx 使用 `t()` 翻译替换硬编码文本
- [ ] SettingsView 添加语言切换下拉框/GitHub/GitLab
- [ ] 检查 dragHandle 与 dnd-kit 事件冲突
- [ ] 窗口文本/App name 翻译

**时间**: 2026-05-01 23:53
**项目路径**: `C:\Users\刘吉\.qclaw\workspace\devdash\`