# DevDash v0.1.0 P0 修复报告

**时间**: 2026-05-01 19:20 CST
**状态**: 全部完成，双编译通过

## 修复清单

### 1. ✅ 新增 Issue List Widget (P0)
- **文件**: `PlaceholderWidgets.tsx` — 新增 `IssueListWidget` 组件
- **文件**: `DashboardView.tsx` — `WidgetRenderer` 添加 `issue_list` case
- 支持展示 Issue 列表（open/closed 状态图标、标题、作者、时间）
- 最多展示 8 条，过滤 `kind === "issue"` 的数据项

### 2. ✅ Ollama 连接测试接真实 API (P0)
- **文件**: `SettingsView.tsx` — `handleTestOllama` 改为调用 `apiTestOllama()`
- 之前是 1500ms setTimeout 假测试，现在走真实 Rust 后端的 `test_ollama` 命令

### 3. ✅ Token 文案诚实化 (P0)
- **文件**: `SettingsView.tsx` — "加密存储" → "明文存储"
- 两处：Token 输入框下方提示 + 添加表单 CardDescription

### 4. ✅ 开机自启动暂时隐藏 (P0)
- **文件**: `SettingsView.tsx` — 开关设为 disabled=true, checked=false
- 描述改为 "即将支持 · 登录系统时自动启动 DevDash"
- `ToggleRow` 组件新增 `disabled` prop，禁用态半透明 + no-cursor

### 5. ✅ Error Boundary (P0)
- **新文件**: `src/components/layout/ErrorBoundary.tsx`
- React Class ErrorBoundary，捕获子组件渲染错误
- 异常时显示错误消息 + "重试"按钮
- **文件**: `App.tsx` — 整个 `<div>` 根节点用 `<ErrorBoundary>` 包裹

### 6. ✅ README + LICENSE (P0)
- **新文件**: `README.md` — 项目介绍、快速开始、技术栈、目录结构、路线图
- **新文件**: `LICENSE` — MIT License

### 7. ✅ Tauri 打包配置 (P1)
- **文件**: `tauri.conf.json` — 添加 publisher, copyright, shortDescription, longDescription

## 编译验证
- `tsc --noEmit` → 零错误 ✅
- `cargo check` → `Finished dev profile` ✅

## 仍待完成
- P1: 拖拽布局持久化保存
- P1: 窗口控制对接 Tauri（最小化/最大化/关闭）
- P2: 插件系统完善
- 端到端真实 GitHub Token 测试
